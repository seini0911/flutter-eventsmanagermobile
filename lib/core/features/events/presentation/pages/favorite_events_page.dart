import 'package:flutter/material.dart';

class FavoriteEventsPage extends StatelessWidget {
  const FavoriteEventsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Favorite Events"),
    );
  }
}